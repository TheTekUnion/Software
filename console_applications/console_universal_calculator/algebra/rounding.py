import math


def round_reg(rational_number, decimal_places):
    return round(rational_number, decimal_places)


def round_ceil(rational_number):
    return math.ceil(rational_number)


def round_floor(rational_number):
    return math.floor(rational_number)